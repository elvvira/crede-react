import UserList from '../user-list/UserList';
import { StyledHeader } from './styled';

const MainContainer = () => {
	return (
		<StyledHeader>
			<UserList />
		</StyledHeader>
	);
};
export default MainContainer;
